# Dependencies
node_modules/

# Secrets — never commit API keys
.env
.env.*

# Generated cache of fetched commits
commits.json
src/commits.json

# Local Claude Code context — not shared on remote
CLAUDE.md
DECISIONS.md
.claude/
